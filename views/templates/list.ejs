<div class="row">
  	<div class="col-lg-12">
        <div id="accordion" role="tablist" aria-multiselectable="true">
        	<div id="card_wrap" class="card">
				<div ng-repeat="board in board_list">
					<div class="card-header" role="tab" id="heading_{{$index}}" board-seq="{{board.seq}}">
						<div class="pull-right"><a href="#" class="label label-info label-fill" ng-click="detail($event, board.seq)">Update</a>
							<a ng-click="toggleModal()" class="label label-danger label-fill">Delete</a>
							</div>
						<a data-toggle="collapse" data-parent="#accordion" href="#collapse_{{$index}}" aria-expanded="false" aria-controls="collapse_{{$index}}" onclick="return false;">
							{{board.title}}
							</a>
						</div>
					<div id="collapse_{{$index}}" class="card-block collapse" role="tabpanel" aria-labelledby="heading_{{$index}}">
						<p ng-bind-html="board.content | nl2br"></p>
						<p>
							<img ng-src="{{file.thumbnail_url_130}}" ng-repeat="file in board.files" ng-show="board.files.length > 0" />
							</p>
						</div>
					</div>
			</div>
        	
        	<a href="#" id="btnReadMore" class="btn btn-info btn-block" ng-click="readMore($event)" style="display:none">Read More</a>
        </div>
    </div>
</div>


<modal-dialog show='modalShown' width='750px' height='60%'>
	<p>정말 삭제 할까요?<p> <br>
	<a href="/">네</a> &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <a href="" >아니요</a>
</modal-dialog>